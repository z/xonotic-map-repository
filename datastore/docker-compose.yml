version: '2'
services:
  db:
    image: postgres:latest
    ports:
      - "5432:5432"
    environment:
      LC_ALL: C.UTF-8
      POSTGRES_DB: map_repo
      POSTGRES_USER: xonotic
      POSTGRES_PASSWORD: password
